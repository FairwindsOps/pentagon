kube

https://github.com/kubernetes/kubernetes/blob/v1.2.4/docs/design/aws_under_the_hood.md
TO SET:
region, it defaults to region=us-west-2
KUBE_AWS_ZONE
AWS_SSH_KEY
VPC_ID

curl -sS https://get.k8s.io > get.sh
export KUBERNETES_PROVIDER=aws
chmod +x get.sh
./get.sh #commented create_cluster
export KUBERNETES_SKIP_DOWNLOAD=1 ; ./get.sh #uncommented create_cluster

getting it to use an existing vpc was hard to see
look at:
function get_vpc_id {
and:
CLUSTER_ID=${INSTANCE_PREFIX}
and:
INSTANCE_PREFIX="${KUBE_AWS_INSTANCE_PREFIX:-kubernetes}"
KUBE_AWS_INSTANCE_PREFIX looks unset by default


VP Tags:
Name: kubernetes-vpc
KubernetesCluster: kubernetes


# quay
robot accounts:
a RW account for circle to push to
  docker login -e="." -u="berrysmoke+quay_rw" -p="" quay.io

a RO account for kube to pull with
